#!/bin/bash

. ~/bin/kbranch 

Find="find . -name .svn -prune -false -o -name system -prune -false"

case $# in
    0) $Find -o -true ;;
    1) $Find -o -name "$1" ;;
    2) $Find -o -name "$1" -exec grep --color -nsH "$2" {} \; ;;
esac
