sudo gem update --system
sudo gem update 

NAME=centos-60
BOX=http://dl.dropbox.com/u/1627760/centos-6.0-x86_64.box
mkdir -p ~/boxes/$NAME
cd ~/boxes/$NAME
vagrant box add $NAME $BOX
vagrant init $NAME

vagrant up
vagrant ssh

FORGE=rpmforge-release-0.5.2-2.el6.rf.x86_64.rpm 
cd /tmp
wget http://pkgs.repoforge.org/rpmforge-release/$FORGE
sudo  rpm -i $FORGE
sudo yum update
sudo yum install git emacs-nox bash-completion
mv ~/.ssh /tmp
rm -rf ~/*
rm -rf ~/.*
git clone git://github.com/massemanet/dotfiles.centos.git ~
mv /tmp/.ssh ~
cd

wget http://alpha.gnu.org/gnu/emacs/pretest/emacs-24.0.93.tar.gz
