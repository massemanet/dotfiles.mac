# on host
# bump the RAM for the box
  config.vm.provider "virtualbox" do |vb|
    vb.memory = "1024"
  end

# on host
# to get X
vagrant ssh -- -X

# as vagrant
sudo useradd -m -G admin -s /bin/bash masse
sudo passwd masse
sudo aptitude install emacs-nox erlang-nox erlang-dev git tmux make
sudo -iu masse

# as masse
# copy id_rsa
mkdir ~/.ssh && cat > ~/.ssh/id_rsa
chmod 400 ~/.ssh/id_rsa
git clone git@github.com:massemanet/dotfiles.centos.git
mv dotfiles.centos/.git ~ && git reset --hard && rm -rf dotfiles.centos
